# 邮箱验证码助手平台化实施进度

更新时间：2026-08-20  
依据：`邮箱验证码助手_平台化建设方案.docx`

## 总体边界

- 默认 EXE 入口只使用平台客户端，不读取邮箱原始账号密码，也不直连邮件代理或 Sub2 管理端。
- 邮箱、卡、上传资源均绑定 `tenant_id + user_id + device_id + task_id + trace_id`。
- Sub2 Token、代理、分组、并发和模型策略只能由服务端 Worker 读取，客户端请求模型不包含这些字段。
- access token 只保存在进程或页面内存；Windows refresh token 使用用户级 DPAPI 和设备绑定 entropy。
- 验证码、PAN/CVV 不进入普通日志或审计详情；上传网络歧义进入 `unknown`，不自动盲目重试。

## Phase 0–6 状态

| 阶段 | 当前判定 | 已有证据 | 仍需完成 |
|---|---|---|---|
| Phase 0：边界确认 | 部分完成 | 已按 `tenant_id`、服务端策略、worker 隔离和敏感字段分类实现默认边界 | 确认真实 Sub2 接口、邮箱接口、卡字段/CVV 策略、step-up 方式和部署域名 |
| Phase 1：平台骨架 | 代码门禁通过 | FastAPI、React、PostgreSQL、Keycloak、Vault resolver、Alembic、CI/security workflow、OpenAPI typed client、Playwright | 在目标服务器完成 Keycloak 导入、Vault policy、TLS 和真实登录烟测 |
| Phase 2：邮箱会话 | 代码门禁通过 | 独立 `worker-mail`、mail session TTL、旧邮件水位、一次消费、GET code、SSE、revoke、脱敏审计 | 接入用户提供的真实邮件接口并做断网/限流/旧验证码现场测试 |
| Phase 3：卡池 | 部分完成 | 事务分配、唯一活动租约、所有权校验、掩码、一次性揭示、TTL、自动清剪贴板和审计 | 明确并实现 Keycloak MFA/平台 PIN/验证码 step-up；确认 CVV 默认禁用策略；完成 PCI 范围评估 |
| Phase 4：Sub2 | 平台闭环完成，业务接入待定 | 独立 `worker-sub2`、Vault secret resolver、HTTP adapter、事务 Outbox、幂等键、不可变策略快照、双人审批、确定性灰度、审计回滚、cancel/reconcile、unknown 语义 | 用真实接口替换通用上传契约；确认额度检查/授权兑换/创建流程并做现场联调 |
| Phase 5：EXE 改造 | 核心闭环完成 | PKCE + loopback、Device Flow 兜底、DPAPI 会话恢复/轮换/撤销、任务 Stepper、卡/验证码临时复制、上传状态、任务历史和 trace_id；正式发行目录隔离并校验 EXE 不含旧邮箱/Sub2 模块 | 使用真实业务页面验证连续复制/粘贴或自动填充流程；补全 step-up 交互 |
| Phase 6：试点发布 | 未完成 | Prometheus/告警、备份恢复脚本、运行手册、发布清单和签字模板；CI 在 Windows Runner 真实构建、检查并按提交 SHA 保存 EXE/SHA-256 制品 | 真实试点用户、恢复演练、Keycloak/Vault 备份、告警触发、培训和回滚演练 |

## 当前自动验证

常规门禁：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\quality_gate.ps1
```

覆盖 Python 编译、平台和 EXE 单元测试、服务边界、桌面发行导入边界、容器加固、备份工具、Nginx 安全头、Keycloak 策略、监控、发布清单、安全工作流、密钥扫描、Alembic SQL、OpenAPI 客户端新鲜度与前端生产构建。`build.ps1` 还会在最终 EXE 上执行 PyInstaller 归档检查并生成 SHA-256 清单。

浏览器门禁：

```powershell
cd frontend
npm run test:e2e
```

Playwright 验证登录、角色导航、任务 trace_id、受保护请求 Bearer 头，以及 access token 不进入 `localStorage`/`sessionStorage`。CI 中使用独立 Chromium job 执行。

## 下一轮必须确认的输入

1. Sub2 是标准 HTTP API 还是网页后台；提供脱敏请求/响应和鉴权说明。
2. 邮箱接口的 URL、鉴权方式、请求/响应样例、限流和验证码邮件规则。
3. 卡字段是否包含 PAN/CVV；若包含，业务是否允许默认不返回 CVV。
4. 卡揭示 step-up 采用 Keycloak MFA、平台 PIN 还是一次性验证码。
5. 生产域名、证书来源、Keycloak/Vault 地址、预计用户数和并发任务数。

## 下一步执行顺序

1. 根据第 4 项完成卡揭示 step-up，先补后端强制校验，再接 EXE UI。
2. 根据真实邮件样例实现并验证生产 Mail Adapter。
3. 根据真实 Sub2 样例实现多阶段 Adapter 和对账查询。
4. 完成 Vault 最小权限 policy，分别授权 `worker-mail`、`worker-sub2` 和卡揭示服务。
5. 在目标环境执行 Phase 6 试点、备份恢复和回滚演练，并将证据写入发布签字表。
