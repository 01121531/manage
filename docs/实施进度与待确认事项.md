# 邮箱验证码助手平台化实施进度

更新时间：2026-08-20  
依据：`邮箱验证码助手_平台化建设方案.docx`

## 总体边界

- 默认 EXE 入口只使用平台客户端，不读取邮箱原始账号密码，也不直连邮件代理或 Sub2 管理端。
- 邮箱、卡、上传资源均绑定 `tenant_id + user_id + device_id + task_id + trace_id`。
- Sub2 Token、代理、分组、并发和模型策略只能由服务端 Worker 读取，客户端请求模型不包含这些字段。
- access token 只保存在进程或页面内存；Windows refresh token 使用用户级 DPAPI 和设备绑定 entropy。
- 验证码、PAN/CVV 不进入普通日志或审计详情；上传网络歧义进入 `unknown`，不自动盲目重试。

## Phase 0–6 状态

| 阶段 | 当前判定 | 已有证据 | 仍需完成 |
|---|---|---|---|
| Phase 0：边界确认 | 部分完成 | 已按 `tenant_id`、服务端策略、worker 隔离和敏感字段分类实现默认边界 | 确认真实 Sub2 接口、邮箱接口、卡字段/CVV 合规范围、Keycloak ACR→LoA 映射和部署域名 |
| Phase 1：平台骨架 | 代码门禁通过 | FastAPI、React、PostgreSQL、Keycloak、Vault resolver、Alembic、OpenAPI typed client、Playwright；生产启动不执行 DDL；Vault policy、AppRole 与 token 文件注入已按服务拆分；非 root HTTPS edge 是唯一 80/443 入口；CI 对 Python/TypeScript 执行 CodeQL SAST，并对 API/Web/Edge 执行构建、SBOM、HIGH/CRITICAL 漏洞、OCI digest、签名和 provenance 门禁；Redis 分布式限流在生产强制启用并纳入 readiness | 在目标服务器完成 Keycloak/Vault/真实证书烟测；由正式 tag 首次实际执行镜像构建、扫描、签名与推送并留存证据 |
| Phase 2：邮箱会话 | 代码门禁通过 | 独立 `worker-mail`、mail session TTL、旧邮件水位、一次消费、GET code、SSE、revoke、脱敏审计；PostgreSQL 行锁 + 活动租约唯一索引防止邮箱并发串号；验证码独立 60 秒 TTL，过期/撤销/关闭会擦除明文；创建时下发 opaque session token、数据库仅存 SHA-256，code/SSE/revoke 均要求 Bearer + token；管理台支持连接器登记、启停和 opaque secret ref 轮换，停用时撤销会话并擦码；HTTP 连接器禁止重定向且响应上限 64 KiB | 接入用户提供的真实邮件接口，并在真实 PostgreSQL 做并发、断网、限流、旧验证码现场测试 |
| Phase 3：卡池 | 代码安全闭环完成，环境待验 | 管理台仅登记提供方引用、掩码元数据和 Vault ref，支持启停；同租户 provider/secret ref 唯一；事务分配、唯一活动租约、所有权校验、掩码；停卡释放租约并取消排队上传；揭示采用主体/设备/租约绑定挑战、新鲜 OIDC MFA ACR、仅存哈希的一次性 grant、TTL、`no-store`、CVV 默认不返回、自动清剪贴板和审计 | 在目标 Keycloak 配置并实测 `urn:email-platform:acr:mfa` 对应的 OTP/WebAuthn LoA；完成 PCI 范围评估 |
| Phase 4：Sub2 | 平台闭环完成，业务接入待定 | 独立 `worker-sub2`、Vault secret resolver、HTTP adapter、事务 Outbox、上游幂等键、不可变策略快照、双人审批、确定性灰度、审计回滚、cancel/reconcile；服务端强制同任务验证码已消费且仍有效才允许首次上传；HTTP adapter 禁止重定向、响应上限 64 KiB，并把两类异常归为 `unknown`；所有不确定结果都不自动重试且只有运维/平台管理员可核对 | 用真实接口替换通用上传契约；确认额度检查/授权兑换/创建流程并做故障注入现场联调 |
| Phase 5：EXE 改造 | 核心闭环完成 | PKCE + loopback、Device Flow 兜底、DPAPI 会话恢复/轮换/撤销、任务 Stepper、卡揭示浏览器 MFA step-up、卡/验证码临时复制、上传状态、任务历史和 trace_id；上传按钮和客户端校验均绑定当前任务的验证码消费状态；在线更新替换前保留旧 EXE，若新版本启动失败会原子恢复；发行目录校验不含旧邮箱/Sub2 模块 | 使用真实业务页面验证连续复制/粘贴或自动填充流程 |
| Phase 6：试点发布 | 未完成 | Prometheus/Alertmanager 规则；平台库 + Keycloak 双库备份包、SHA-256 清单和关键表恢复核对；备份 schema v2 可绑定 release tag/commit/migration head/container manifest SHA；回滚执行器会实际验证 Cosign/SBOM attestation/provenance，拉取精确 OCI digest，恢复同一双库包，核对运行镜像与内部健康后最后开放 edge，失败保持 edge 关闭；Vault Raft 快照原子生成、SHA-256/CLI inspect 校验、显式确认恢复工具与隔离恢复手册；结构化审计证据、租户内筛选与不含 details 的脱敏 CSV；操作员/运维管理员/安全审计员/平台管理员培训与五类桌面推演已有闭 schema、独立复核、原子发布和 SHA-256 证据工具；CI 会真实串联登录、任务、卡、邮箱、一次性验证码、上传 Outbox、关闭释放和审计回放，生成原子发布且可自校验的 Phase 6 预演证据，固定标记 `production_acceptance=false`；Tag Release 会按提交重新校验证据并将 JSON 与文件 SHA-256 作为稳定发布资产；运行手册、发布清单和签字模板；CI 构建 EXE/SHA-256 制品 | 配置真实告警接收端并完成试点用户、真实双库与 Vault 隔离恢复演练、告警送达、由真实人员签署的培训/桌面推演和回滚演练；模拟命令顺序与 CI 预演不得替代目标环境证据 |

## 当前自动验证

常规门禁：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\quality_gate.ps1
```

覆盖 Python 编译、平台和 EXE 单元测试、验证码上传前置条件、Redis 限流、审计导出、服务边界、桌面发行导入边界、容器加固、Vault token/路径隔离、HTTPS edge、备份工具、不可变镜像与 release-bound 双库回滚契约、角色培训/桌面推演闭 schema 与独立复核证据、Nginx 安全头、Keycloak 策略、监控、发布清单、CodeQL/容器供应链工作流结构、密钥扫描、Alembic SQL、OpenAPI 客户端新鲜度与前端生产构建。`build.ps1` 还会在最终 EXE 上执行 PyInstaller 归档检查并生成 SHA-256 清单。

浏览器门禁：

```powershell
cd frontend
npm run test:e2e
```

Playwright 验证登录、角色导航、任务 trace_id、受保护请求 Bearer 头，以及 access token 不进入 `localStorage`/`sessionStorage`。CI 中使用独立 Chromium job 执行。

## 下一轮必须确认的输入

1. Sub2 是标准 HTTP API 还是网页后台；提供脱敏请求/响应和鉴权说明。
2. 邮箱接口的 URL、鉴权方式、请求/响应样例、限流和验证码邮件规则。
3. 卡字段是否包含 PAN/CVV，以及 PCI 合规边界；当前 API 默认永不返回 CVV。
4. 目标 Keycloak 中 `urn:email-platform:acr:mfa` 应映射 OTP 还是 WebAuthn LoA。
5. 生产域名、证书来源、Keycloak/Vault 地址、预计用户数和并发任务数。

## 下一步执行顺序

1. 在目标 Keycloak 导入/配置 ACR→LoA 映射并对 OTP/WebAuthn step-up 做真实烟测。
2. 根据真实邮件接口样例实现生产 Mail Adapter，并在真实 PostgreSQL 验证并发租约、断网、限流和验证码清理。
3. 根据真实 Sub2 样例实现多阶段 Adapter，验证上游幂等查询与 `unknown` 核对流程。
4. 在目标 Vault 应用仓库内 policy/AppRole 模板，验证 `worker-mail`、`worker-sub2` 和卡揭示 API 的跨路径访问均被拒绝，并演练 token 轮换。
5. 配置真实 Alertmanager 接收端，在目标环境执行 Phase 6 试点、备份恢复和回滚演练，并将证据写入发布签字表。
